import '../../scss/app.scss'
import * as bootstrap from 'bootstrap'

import App from '../../CO2-emission.svelte'

const app = new App({
  target: document.getElementById('app'),
})

export default app
